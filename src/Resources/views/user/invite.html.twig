{% extends 'base.html.twig' %}

{% block title %}
    {{ 'parthenon.user.invite.title'|trans }}
{% endblock %}

{% block body %}
    {{ form_start(form,  {'attr': {"class": "form-profile-edit"}}) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>
    {{ form_widget(form, {'attr': {"class": "form-control"}}) }}
    <button class="btn btn-lg btn-primary" type="submit" id="user_invite_submit">{{ "parthenon.user.invite.submit"|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
